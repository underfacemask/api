const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'src/configs/db.js'),
  'models-path': resolve(__dirname, 'src/apps/models'), // ou ajuste se não usar models
  'migrations-path': resolve(__dirname, 'src/database/migrations'),
  'seeders-path': resolve(__dirname, 'src/database/seeders'), // adicione essa pasta se ainda não existir
};
